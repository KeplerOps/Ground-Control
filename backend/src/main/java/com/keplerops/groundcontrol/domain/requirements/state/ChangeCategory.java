package com.keplerops.groundcontrol.domain.requirements.state;

/** Categories of changes tracked in the audit timeline. */
public enum ChangeCategory {
    REQUIREMENT,
    RELATION,
    TRACEABILITY_LINK
}
