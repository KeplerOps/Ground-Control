package com.keplerops.groundcontrol.domain.assets.state;

public enum AssetLinkType {
    IMPLEMENTS,
    MITIGATES,
    SUBJECT_OF,
    EVIDENCED_BY,
    GOVERNED_BY,
    DEPENDS_ON,
    ASSOCIATED
}
